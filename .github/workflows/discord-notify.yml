name: Kirim Notif ke Discord

on:
  push:
    branches:
      - main

jobs:
  notify-discord:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # WAJIB: Mengambil seluruh riwayat agar git log bisa membandingkan commit

      - name: Kirim pesan ke Discord mirip GitHub log
        env:
          # PERBAIKAN: Menyelaraskan nama variabel dengan Secret asli Anda
          CHANGELOGS_DISCORD: ${{ secrets.CHANGELOGS_DISCORD }}
        run: |
          # 1. Menentukan rentang commit yang baru di-push
          BEFORE_SHA="${{ github.event.before }}"
          AFTER_SHA="${{ github.event.after }}"
          
          if [ "$BEFORE_SHA" == "0000000000000000000000000000000000000000" ]; then
            RANGE="-1"
          else
            RANGE="$BEFORE_SHA..$AFTER_SHA"
          fi

          # 2. Ambil data pendukung
          AUTHOR_AVATAR="$(curl -s https://github.com{{ github.actor }} | grep avatar_url | cut -d '"' -f4)"
          REPO_NAME="${{ github.repository }}"
          BRANCH="${{ github.ref_name }}"
          
          # 3. Hitung jumlah commit dalam push ini
          COMMIT_COUNT=$(git rev-list --count $RANGE)
          
          # 4. Menggunakan format git log murni teks biasa tanpa link markdown
          COMMIT_LIST=$(git log $RANGE --pretty=format:"\`%h\` %s - %an" | sed ':a;N;$!ba;s/\n/\\n/g')

          # 5. Struktur JSON bersih tanpa baris URL tautan ke repositori
          PAYLOAD=$(cat <<EOF
          {
            "embeds": [{
              "author": {
                "name": "${{ github.actor }}",
                "icon_url": "$AUTHOR_AVATAR"
              },
              "title": "[$REPO_NAME:$BRANCH] $COMMIT_COUNT new commit(s)",
              "description": "$COMMIT_LIST",
              "color": 7506394
            }]
          }
          EOF
          )

          # 6. Kirim ke Discord menggunakan token dari secret yang benar
          echo "Mengirim $COMMIT_COUNT commit ke Discord..."
          curl -H "Content-Type: application/json" \
               -X POST \
               -d "$PAYLOAD" \
               "$CHANGELOGS_DISCORD"
